import 'package:flutter/material.dart';

class Operations Extends class name extends StatelessWidget {
  const name({Key key}) : super(key: key);
  
  String titleOperation = "Permutacion";
  String description = "Es una operacion.....";
  bool iconOperation = true;

  Operations(this.titleOperation, this.description, this.iconOperation);

  @override
  Widget build(BuildContext context) {
    
    
    return Container(
      child: Row(

      )
    );
  }
}